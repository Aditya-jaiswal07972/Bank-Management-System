#!/bin/bash

# Navigate to the directory where your Python script resides
cd /home/user/Project

# Install any required dependencies using pip
# Assuming you already have captcha.ttf in the repository, no need to install extra dependencies here

# Generate the captcha image and run the Python script
python python-captcha.py

# Optionally, you can capture the output of the script and save it to a file
# This can be useful for analyzing the results later
# python python-captcha.py > output.txt

# Exit with the appropriate exit code based on the success or failure of the script
# You can customize the exit code based on your requirements
exit_code=$?
exit $exit_code
